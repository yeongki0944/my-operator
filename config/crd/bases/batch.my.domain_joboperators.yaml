---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: joboperators.batch.my.domain
spec:
  group: batch.my.domain
  names:
    kind: JobOperator
    listKind: JobOperatorList
    plural: joboperators
    shortNames:
    - jo
    singular: joboperator
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: JobOperator is the Schema for the joboperators API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: JobOperatorSpec defines the desired state of JobOperator.
            properties:
              image:
                description: Image is the container image to deploy
                type: string
              port:
                description: Port is the port the container listens on
                format: int32
                maximum: 65535
                minimum: 1
                type: integer
              replicas:
                description: Replicas is the number of replicas to deploy
                format: int32
                maximum: 10
                minimum: 1
                type: integer
            required:
            - image
            type: object
          status:
            description: JobOperatorStatus defines the observed state of JobOperator.
            properties:
              readyReplicas:
                description: Ready replicas count
                format: int32
                type: integer
              replicas:
                description: Total replicas count
                format: int32
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
